import { JSX } from "react";

const ContactSection = (): JSX.Element => {
  return (
    <footer className="bg-el-bg container-width shade" id="contact">
      contact
    </footer>
  );
};

export default ContactSection;
